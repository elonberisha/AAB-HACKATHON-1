// TODO: Chat widget global — floating button (bottom-right), drawer me SSE streaming
// Props: sessionId (nga localStorage), gjuha aktive (sq/en/sr)
// Buton voice hap Vapi call me sessionId
'use client'
export function ChatWidget() {
  return null
}
